import { Component } from "@angular/core";

@Component({
    selector: "app-stats",
    templateUrl: 'app.stats.html',
    styleUrls: ['app.stats.sass']
})

export class StatsComponent {
    message = "Wow, such empty..."
}