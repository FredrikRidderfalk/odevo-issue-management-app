import { Component } from "@angular/core";

@Component({
    selector: "app-upcoming-tasks",
    templateUrl: 'app.upcoming-tasks.html',
    styleUrls: ['app.upcoming-tasks.sass']
})

export class UpcomingTasksComponent {
    message = "Wow, such empty..."
}