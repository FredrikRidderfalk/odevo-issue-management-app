import { Component } from "@angular/core";

@Component({
    selector: "app-navigation",
    templateUrl: 'app.navigation.html',
    styleUrls: ['app.navigation.sass']
})

export class NavigationComponent {
    searchPlaceholder = "Search by name, address, or Brf..."
}